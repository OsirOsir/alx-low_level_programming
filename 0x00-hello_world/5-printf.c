#include <stdio.h>
/**
 * main - A program that prints our a line with printf function
 * Return:0 (Which is a Succes)
 */
init main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
